pages:
  image: alpine:latest
  stage: deploy
  script:
    - mkdir -p public
    - cp index.html 404.html robots.txt .nojekyll _headers public/
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == "main"
